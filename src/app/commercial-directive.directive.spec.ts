import { CommercialDirectiveDirective } from './commercial-directive.directive';

describe('CommercialDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CommercialDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
